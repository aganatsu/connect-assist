Redeploy the `paper-trading` edge function so any recent local changes go live.

## Step
1. Deploy `paper-trading` via the managed deploy tool (equivalent to `supabase functions deploy paper-trading`).
2. Report deploy status back.

No code changes. No config changes. Read-only otherwise.