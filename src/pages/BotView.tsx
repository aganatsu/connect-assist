import { AppShell } from "@/components/AppShell";

const BotView = () => (
  <AppShell>
    <div>
      <h1 className="text-2xl font-bold mb-4">Bot</h1>
      <p className="text-muted-foreground">Bot status & monitoring (read-only) — coming soon.</p>
    </div>
  </AppShell>
);

export default BotView;
