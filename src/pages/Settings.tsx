import { AppShell } from "@/components/AppShell";

const SettingsPage = () => (
  <AppShell>
    <div>
      <h1 className="text-2xl font-bold mb-4">Settings</h1>
      <p className="text-muted-foreground">App configuration & preferences — coming soon.</p>
    </div>
  </AppShell>
);

export default SettingsPage;
