import { AppShell } from "@/components/AppShell";

const Dashboard = () => (
  <AppShell>
    <div>
      <h1 className="text-2xl font-bold mb-4">Dashboard</h1>
      <p className="text-muted-foreground">KPI cards, equity curve, open positions — coming soon.</p>
    </div>
  </AppShell>
);

export default Dashboard;
