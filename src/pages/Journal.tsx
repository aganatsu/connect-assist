import { AppShell } from "@/components/AppShell";

const Journal = () => (
  <AppShell>
    <div>
      <h1 className="text-2xl font-bold mb-4">Journal</h1>
      <p className="text-muted-foreground">Trade journal with entry/exit logging — coming soon.</p>
    </div>
  </AppShell>
);

export default Journal;
