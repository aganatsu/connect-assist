import { AppShell } from "@/components/AppShell";

const Backtest = () => (
  <AppShell>
    <div>
      <h1 className="text-2xl font-bold mb-4">Backtest</h1>
      <p className="text-muted-foreground">Strategy backtesting & results — coming soon.</p>
    </div>
  </AppShell>
);

export default Backtest;
