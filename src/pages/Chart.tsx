import { AppShell } from "@/components/AppShell";

const Chart = () => (
  <AppShell>
    <div>
      <h1 className="text-2xl font-bold mb-4">Chart</h1>
      <p className="text-muted-foreground">TradingView chart with instrument selector — coming soon.</p>
    </div>
  </AppShell>
);

export default Chart;
