# ─── SMC Trading Bot — Local Runner Environment ───
# Copy this file to .env.local and fill in your values.
# NEVER commit .env.local to git.

# ─── Supabase (your existing project) ───
SUPABASE_URL=https://istpcfaokubxlualybhp.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...your-service-role-key...

# To find your service_role_key:
#   Supabase Dashboard → Settings → API → service_role (secret)
#
# The optimizer-local.ts script starts backtests on the edge function
# but waits up to 30 minutes per backtest (no timeout issues).
